<?xml version="1.0" encoding="utf-8" ?>
<templates xml:space="preserve">
    <t t-inherit="mail.MessageActionList" t-inherit-mode="extension" owl="1">
        <xpath
            expr="//div[hasclass('o_MessageActionList')]//t[@t-foreach='messageActionList.messageActionViews']"
            position="after"
        >
             <!-- Show the "Forward Message" button only on messages, not on notes. -->
            <t t-if="messageActionList.message.is_discussion">
                <t t-if="!messageActionList.message.is_note">
                    <ForwardMessage message="messageActionList.message" />
                </t>
            </t>
        </xpath>
    </t>
</templates>
